//! tskmstr library: configuration, provider integrations and the shared
//! aggregation/grouping logic used by both the `tskmstr` CLI and the
//! `tskmstr-tray` desktop widget.

pub mod config;
pub mod control;
pub mod output;
pub mod providers;
